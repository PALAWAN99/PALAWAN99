'use client';

import { GateDeviceSettingsClient } from './_components/GateDeviceSettingsClient';

/** /admin/settings — จัดการ IP อุปกรณ์ประตูจาก gatemaster (Pennueng SQL Server) */
export default function SettingsPage() {
  return <GateDeviceSettingsClient />;
}
