#!/bin/sh
# หลัง commit สำเร็จ — พยายามบันทึก changelog จากข้อความ commit ล่าสุด (ไม่ทำให้ commit ล้มถ้า script error)
cd "$(dirname "$0")/.." || exit 0
if [ -f frontend/package.json ]; then
  (cd frontend && npm run changelog:auto --silent) 2>/dev/null || true
fi
exit 0
